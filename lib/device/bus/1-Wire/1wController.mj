

interface 1wController {


    read_bit(u8 *bit);
    write_bit(u8 bit);


    read(u8 *data, u32 size, u32 bits);
    write(const u8 *data, u32 size, u32 bits);


    Task read(u8 *data, u32 size);
    Task write(const u8 *data, u32 size);
}
